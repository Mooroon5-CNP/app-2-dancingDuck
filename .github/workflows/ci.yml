name: CI

on:
  push:
    branches: [main, prod]

permissions:
  contents: read
  id-token: write

jobs:
  pipeline:
    uses: Mooroon5-CNP/ci-templates/.github/workflows/pipeline.yml@main
    with:
      app_name: "app-2-dancingduck"
      app_port: "8080"
      config_repo_url: "https://github.com/Mooroon5-CNP/config-repo"
    secrets:
      CONFIG_REPO_TOKEN: ${{ secrets.CONFIG_REPO_TOKEN }}
